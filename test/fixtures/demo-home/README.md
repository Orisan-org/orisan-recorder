A fabricated home directory, used so the published showcase recording and the
UI screenshots can demonstrate discovery without putting the recorder author's
own machine — username, installed clients, project names, process ids — into a
public artefact.

Nothing here is real. `orisan-rec scan` with no arguments reads your actual
machine; `--home` points it at a root like this one.
